package ru.pravvich.tic_tac.game;

/**
 * Init user's array.
 * Player which move first, is playing for the 'x'.
 */
interface ChoiceSide {
    /**
     * Init user's array.
     * Player which move first, is playing for the 'x'.
     */
    void choiceSide();
}

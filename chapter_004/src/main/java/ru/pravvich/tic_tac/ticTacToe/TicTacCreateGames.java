package ru.pravvich.tic_tac.ticTacToe;

import ru.pravvich.tic_tac.input.Input;

/**
 * Init all round which may by played.
 */
interface TicTacCreateGames {
    /**
     * Init all round which may by played.
     * @param input stream for console in.
     */
    void createGames(Input input);
}
